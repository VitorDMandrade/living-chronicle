# Diretriz de Git e Publicação

- Sempre que finalizar alterações ou correções solicitadas pelo usuário, faça o commit e envie (push) as mudanças diretamente para o repositório remoto no GitHub (`origin main`).
- Mantenha mensagens de commit semânticas e claras (ex.: `fix(layout): ...`, `feat: ...`).
